[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[functions."analyze"]
  timeout = 60

[functions."recommend"]
  timeout = 60

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
